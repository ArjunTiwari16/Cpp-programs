#include<iostream>
using namespace std;
int main(){
    int a;
    cout<<"Enter number";
    cin>>a;
    //(condition) ? if true : if false ;
    (a%2==0) ? cout<<"even number" : cout<<"odd number" ;
}